{
  "name": "phd-opportunity-finder",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "dev": "cd frontend && npm install && npm run start",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run start"
  }
}
